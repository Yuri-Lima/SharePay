# SharePay Post-Migration Feature Evaluation Sync (Shared Agent Workspace)

**Protocol (all agents MUST follow):**
- Read this file at start of your work (use read_file tool).
- Your sole deliverable is **evaluation + sync**. Do NOT write production code, do NOT edit app sources, tests, or Docker except this file if your analysis adds material new insight.
- Append a new section **ONLY IF** you have substantive, non-duplicative analysis (new risks to calc parity, hidden cross-file deps, complexity deltas, verification ideas, or conflicts with MIGRATION.md/FINAL_REVIEW.md/PERFORMANCE_SECURITY_AUDIT.md or original coresharepay.py invariants). 
- If no material delta: output only "No update necessary - analysis aligns with existing entries."
- When appending: use a unique delimiter `## AGENT-EVAL: <FEATURE-NAME> @ <ISO-TIMESTAMP>` then structured content:
  - Analysis (deep, cites specific files/lines or old logic)
  - Risks (esp. exact output drift, validation bypass, left_over sentinel handling, sub-kwh strict < rule, date range destructive deletes)
  - Your adjusted Priority (P0-P3) / Complexity (L/M/H)
  - Recommended minimal verification steps (must include re-running calculator unit + at least one full roundtrip)
  - Open questions / assumptions
- At very end of your response, print a 3-sentence executive summary.
- Be aware of known LLM failure patterns on this exact codebase style (from prior "manager" prompt): semantic drift on "preserve exact", incomplete wiring, over-abstraction, weak self-review on numeric outputs, subagent desync, ignoring that web currently uses localStorage mock while api has full surface.
- Context: Recent commit 72466da delivered the full Nx/Nest/Angular + pure BillCalculator port after a similar sub-agent heavy strict prompt. Current gap is integration + tests + small issues.

Initial entries will be added by the 4 specialist agents.

---
**Current known baseline (orchestrator pre-populated):**
- Backend: houses.controller.ts + houses.service.ts (full methods + strict validations + loadFullHouseForCalc + POST /:id/calculate) appear complete and mirror Django + CoreSharePay rules (see setSubKwh sum check < main, tenant range vs bill, destructive on bill date change).
- Calculator: libs/calculator/src/lib/calculator.ts + types.ts is the single source of truth (ported calc1/calc2/splitBill/getTenantsByDay with left_ sentinels, Decimal prec 8->3, formatEuro). Tests exist.
- Frontend gap: apps/web/src/app/core/houses.service.ts is a self-contained localStorage + direct BillCalculator mock (STORAGE_KEY, createHouse, setBill local, calculate() that builds CoreInput client-side). house-detail.component.ts and calc-report.component.ts consume the mock svc, not the real HTTP API (despite auth.interceptor + core/auth.service existing).
- GitHub issues: #70 FAQ, #18 README, #13 Print PDF, #12 Auto-fill dates.
- From FINAL_REVIEW/MIGRATION: "partial wiring", "full CRUD + report rendering in flight", "E2E add 3-4 journeys", "real calculate endpoint loading aggregate".
- Invariant: Any change touching calc path or input mapping MUST keep outputs identical for same inputs (including left_over aggregation, sub partitioning, divide-by-1, €.xxx 3dp).

(End of initial. Agents: append below only on material findings.)

## AGENT-EVAL: REAL-API-WIRING @ 2026-06-16T10:00:00.000Z
**Analysis (deep, cites specific files/lines or old logic):**
- Full user flow (create via API, bill/kwh/tenants/subs with server validation, real calc, report from server result + JWT) is completely blocked. apps/web/src/app/core/houses.service.ts (entire 260 lines) is pure standalone mock: STORAGE_KEY localStorage, BehaviorSubject, createHouse (assigns user_id:1), setBill (client Date math + destructive tenants clear at lines 85-94), setKwh (diff or direct lines 104-107), addTenant/addSubTenant (capitalize + range checks lines 121-129,187-193), setSubKwh (sum check lines 165-169 with exact error strings), calculate() (builds CoreInput lines 228-246 from snake_case House, then `new BillCalculator(input).calculate()` lines 249 + attaches fake user). Components (apps/web/src/app/features/houses/house-detail.component.ts:166 calls setBill etc; 161/197/203/213 use standalone ngModel for subs + svc calls; calc-report.component.ts:161 calls svc.calculate and uses mainHouseEntries etc mappers on r.main_house/r.sub_house_with/r.left_over1 etc matching old calc_main_house.html) never touch HttpClient for domain data.
- Auth layer skeleton unused: auth.interceptor.ts:5-12 implements bearer from AuthService.getToken(), but apps/web/src/app/app.config.ts:9 does `provideHttpClient(withInterceptors([]))` (empty array, interceptor never runs). core/auth.service.ts:23-49 does only fake btoa(JSON) tokens + localStorage (comments admit "Simulate", no real this.http.post to environment.apiUrl), no call to real Nest /api/auth/login (auth.controller.ts:17 + auth.service.ts:40-45 which does bcrypt + jwtService.signAsync). JwtStrategy (apps/api/src/auth/strategies/jwt.strategy.ts:17) validates real signed JWT against users table by sub; fake tokens will 401 on any @UseGuards(JwtAuthGuard) house op (controller.ts:26).
- Backend surface complete per prompt: houses.controller.ts exposes full nested (POST /:id/bill, /:id/kwh, /:id/tenants, /:id/sub-houses, /:id/sub-houses/:subId/kwh, /:id/sub-houses/:subId/tenants, POST /:id/calculate:168 which does loadFullHouseForCalc + calculatorService.runCalculationForHouse). houses.service.ts (Nest) has exact ports: destructive deletes on bill date change (116-122), subKwh strict sum< (295-300 with near-identical error strings), tenant/sub range vs bill + days>0 + capitalizeName from @sharepay/shared, loadFull at 358-375.
- Shape & contract mismatch (core hidden dep): Angular House interface + all templates/bindings (house-detail:31 h.house_name, h.bill.amount_bill/start_date_bill, h.kwh.last_read_kwh, h.tenants.house_tenant/start_date, sh.sub_house_name/sub_kwh?.sub_kwh, st.sub_house_tenant/sub_start_date; calc-report mappers 169-206 on same) + CoreInput builder expect snake_case (see houses.service.ts:230 house.bill.amount_bill, 236 t.house_tenant, 240 sub_start_date, 245 sub_kwh). But entities (house.entity.ts:30 houseName, house-bill.entity.ts:22 amountBill/25 startDateBill, house-tenant.entity.ts:22 houseTenant/26 startDate, sub-house.entity.ts:26 subHouseName, sub-tenant:28 subHouseTenant/30 subStartDate, sub-kilowatt:26 subKwh) + direct returns + calculator.service.ts:34 (subHouseNames, 47 sub_start_date mapping from DB camel) + houses.service Nest:67 produce camelCase JSON. DTOs use snake for inputs (create-bill.dto.ts:8 amount_bill) but no response transform. Also request bodies from UI forms (billForm amount/start not amount_bill) will need mapping.
- Other cross-file: proxy.conf.json:3 targets :8000 (old Django), Nest main.ts:20-23 uses 3000 + app.setGlobalPrefix('api'); environment.ts:3 uses '/api'. shared daysBetween (libs/shared/src/index.ts:88) vs client Math.floor in mock. report never hits real POST calculate. subKwh UI uses hybrid reactive+standalone ngModel (house-detail:85,93) + per-sub dicts that must be repopulated on http reload.
- Calculator parity path (libs/calculator/src/lib/calculator.ts + types.ts) and calculator.service.spec.ts:96 (direct BillCalculator vs run via service) is solid; server calc delegates exactly (no client math on report path post-wiring).
- From MIGRATION.md:140-148 (UI flows), FINAL_REVIEW:36 ("Wire full HousesService"), 40 ("Real calculate endpoint"), PERFORMANCE:78 (E2E calc parity via real endpoint + golden).

**Risks (esp. exact output drift, validation bypass, left_over sentinel handling, sub-kwh strict < rule, date range destructive deletes):**
- Output drift: if any client-side calc remains (current houses.service:249) or if CoreInput built from fetched camel data doesn't exactly match what loadFullHouseForCalc + calculator.service:66-77 produces (tenant key `${id}-${name}`, subKwhs sub_house_kwh_FK shape, left_ sentinels in getTenantsByDay, divide-by-1, prec=8->round 3, formatEuro), report € values/tables will differ from golden (coresharepay.py + old templates). Even "same inputs" can drift on date string vs parsed, or id types.
- Validation bypass or double: client validations (mock svc) can reject valid server cases (or accept invalid) due to duplicate code + slight string diffs (e.g. "Out of Range - " hyphen/space, "greatter" spelling in both but server has "Registreded" typo at 296). Destructive tenant delete on bill date change only server-enforced (Nest 115); client clears locally but on switch must refetch to observe.
- Sub-kwh strict < : enforced only on server setSubKwh (if sum >= mainKwh throw specific BadRequest); client setSubKwh does parallel check but UI saveSubKwh:199 calls it before API. If not wired, bypass possible. left_over1 shape in calc1/calc2 (calculator.ts:345-359, 444-456) and report leftOverEntries:195 must stay identical.
- Auth/ownership: without real JWT + interceptor, all CRUD + /calculate 401/403; user checks in every houses.service Nest method (e.g. findOneOwned 63) will fail cross-user.
- Other drift sources: days calc TZ (new Date() in client vs ISO string compare + shared util), amount parse (client 74 vs Nest 108), name capitalize (both use similar but shared is canonical), no persistence means e2e state lost between runs.
- Known LLM pattern risk: claiming "parity" after wiring only CRUD but leaving report/calculate on local BillCalculator, or ignoring shape adapters.

**Adjusted Priority (P0-P3) / Complexity (L/M/H):** P0 / H
- (Was implicitly P0 from FINAL; complexity H due to adapter layer + auth full real + reload patterns + config + error translation needed for minimal viable wiring without breaking UI parity.)

**Recommended minimal verification steps (must include re-running calculator unit + at least one full roundtrip):**
- Re-run `nx test calculator` + `nx test api --testPathPattern=calculator.service` (confirms direct vs service roundtrip with fixture aggregate producing identical main_house/sub_*/left_over1/new_* as in spec:97).
- Full input->output roundtrip assertion (post real wiring): (a) use real register/login via /api/auth (store valid JWT), (b) POST create house, (c) POST /:id/bill + /kwh + /tenants + /sub-houses + /sub-houses/:subId/kwh + /sub-houses/:subId/tenants (with date ranges inside bill, subKwh sum < main), (d) POST /:id/calculate, (e) deep-equal the returned shape+values (kwh, bill_value, new_amount, new_main_kwh, main_house keys with €.xxx 3dp, sub_house_without/with, left_over1 details with left_ sentinels, no negative/NaN) to a direct `new BillCalculator( mappedCoreInputFromResponse ).calculate()` using exact same data. Assert e2e Playwright journey hits real endpoint (not localStorage) and matches golden fixture values from MIGRATION/FINAL_REVIEW sample.
- Also: typecheck, lint, manual flow in browser with api+db up, confirm interceptor attaches (check network headers), error banners for BadRequest shapes, destructive clear observable after bill date change, sub sum >= rejects with server msg.
- Must cover no client math on critical calculate path.

**Open questions / assumptions:**
- Will we add response/request mappers in houses.service (or change Angular House interface + all  bindings to camel) or backend @SerializeOptions/transform? (affects "exact" old template parity).
- Proxy update + real DB seed for e2e assumed necessary.
- Assume no change to BillCalculator or calc output shape allowed.
- Does list GET provide enough for house-list (it loads only bill/kwh partial); detail always refetches full?
- JWT secret/expiry and test user provisioning for CI e2e not yet detailed in docs.


## AGENT-EVAL: E2E-PARITY-TESTS @ 2026-06-16T18:45:00Z
**Analysis (deep, cites specific files/lines):**
- Started per protocol by reading FEATURE_EVAL_SYNC.md (baseline explicitly calls out frontend gap: core/houses.service.ts mock + localStorage + direct BillCalculator; house-detail.component.ts:148 and calc-report.component.ts:161 always call svc.getById/calculate which builds CoreInput client-side and invokes lib, never HTTP). Then read current e2e (apps/web-e2e/src/example.spec.ts:11-52: full "signup+create house+bill+kwh+nav to /calc" but all via in-mem; checks only presence of € and "Main House..." heading, no exact value parity, no left_over, no table row counts, no "id-Name" keys). playwright.config.ts:7-14: baseURL=4200, webServer only "nx serve web", no api, no db, reuseExistingServer. 
- Read house-detail/calc-report (above), Nest path: houses.controller.ts:168-172 (POST :id/calculate does loadFullHouseForCalc + calculatorService.runCalculationForHouse), houses.service.ts:358-375 (loadFull with explicit relations: bill,kilowatt,tenants,subHouses,subHouses.kilowatts,subHouses.tenants; setBill:114-122 does destructive tenant delete on date change; setSubKwh:295-300 enforces sum < main strict, rejects ==), calculations/calculator.service.ts:12-84 (runCalculationForHouse:20-77 input mapping: parseFloat amountBill, parseInt kwh, tenantsMain: houseTenant->house_tenant, subTenants subHouseTenant->sub_..., subKwhs: sub.kilowatts[0] + FK {sub_house_name, id}, then new BillCalculator(input).calculate()).
- calculator.spec.ts (lib): baseInput with mixed subs, 3 its covering structure, sub isolation, left_over when partial tenants (but only asserts new_main_kwh prop, no deep € or full output). api/calculations/calculator.service.spec.ts:23-98: mocks entities, calls run..., asserts shape + kwh parity to direct, but no DB, no real load, no auth. api/houses/houses.service.spec.ts: minimal. MIGRATION.md:145-154 + 130 (E2E Playwright full flows + calc output exact match; "golden fixtures"; "real calculate endpoint"); FINAL_REVIEW.md:38 (explicit: "add 3-4 journeys (create house → bill+kwh+2 tenants → add sub + kwh + tenant → calc → assert exact € values + table rows)"), 53 ("real fixture round-trip"), 22 ("Calculator tests + golden intent prove the hardest part"); docker-compose.yml:14-33 (postgres always for api, health, no test profile); app.module.ts:32-42 (sqlite :memory: + dropSchema only if NODE_ENV=test or no DB_URL).
- Used grep/list_dir/read to map all critical paths needing journeys: happy (main+2subs mixed kwh: matches baseInput in calculator.spec + api calc svc spec; sub_with isolated bill_value+kwh_infor, without in main pool, new_main_kwh deduction); edges from invariants + py (overlapping tenantsMain: getTenantsByDay union in calculator.ts:206-219 + 236-249; partial coverage -> left_ sentinels in createRange+get+calc1/calc2:211,241 + aggregation 345-359/442-456; subkwh boundary just under main: enforced pre-calc in houses.service:295 but calc splitBill:87-89 does newMainKwh minus; zero tenants day: divide-by-1 at 291,302,394,406; date change destructive: setBill:115); also zero-days divide, 3dp roundTo+formatEuro, output keys "id-Name" (e.g. calculator.ts:317,333), period strings, left_over1 shape (per house {left_over1, days_left_over, details_date}).
- CalcReportComponent renders exact tables from result shape (mainHouseEntries etc iterate main_house/sub_house_without/sub_house_with/left_over1); any structure drift in API response vs mock would break DOM parity.

**Risks (esp. exact output drift, ...):**
- Drift specifically in input mapping inside CalculatorService.runCalculationForHouse (lines 25-64): tenant id/keying, sub_house_kwh_FK.id presence (used? in lib not directly but shape), parse vs string amounts, could produce different new_main_kwh/left_over vs direct lib or old py even if units green.
- Relation loading failure (houses.service.ts:361-368 incomplete eager loads) -> subHouses.tenants or .kilowatts empty -> calc produces {} for sub_house_with or missing left_over1 vs expected from coresharepay.py invariants (without-kwh subs participate in main pool, with-kwh have isolated + kwh_infor).
- Auth guard bypass or token mismatch: controller @UseGuards(JwtAuthGuard) everywhere incl /calculate (controller:26), but current e2e + frontend use fake btoa tokens from auth.service.ts:28 (no real /auth/register hitting bcrypt+jwt in api/auth.service.ts:17-29 + strategies). Existing example.spec would fail against real if proxy/auth wired.
- Rounding/structure diff in report tables: e.g. bill_value '€120' vs '€120.000', left_over1 aggregation details vs flat (calc1 vs calc2 paths), or period_bill format.
- Units pass (lib + api calc svc in-mem) but full stack (real HTTP+TypeORM persist+load+map) differs on left_over shape, sub partitioning, or destructive side-effects (date change).
- Current e2e does not exercise real backend at all (per context + findings); playwright only web + mock houses svc means 0 coverage of /calculate parity or DB invariants. Flakiness: dates (parseISO/addDays), DB state (need per-test isolation), cross-process for e2e+api (sqlite :memory: in app.module not shareable across playwright child + api server processes).
- Sub kwh strict < rule enforced only in service (not reaching calc if fails), zero-tenant days, overlapping.

**Adjusted Priority (P0-P3) / Complexity (L/M/H):**
- P0 / High (H). Matches migration's #1 obsession (100% exact calc outputs, "E2E Playwright for comprehensive headless", "golden fixture py script", "calculator tests + golden intent prove the hardest part (the math) is correct", "re-run multiple times until 100% green"). But real E2E parity is the integration that actually ships (the controller->service->load->CalculatorService path + Angular consumption) vs pure lib units. Complexity H due to auth (real JWT vs current fake), data seeding/teardown (full house lifecycle or API requestContext), exact € string + nested DOM asserts on dynamic tables, running real api (testcontainers/docker compose in CI vs current nx web-only), date/DB flakiness, and current decoupling (frontend not calling real houses/calc endpoints; proxy.conf.json targets :8000 not 3000).

**Recommended minimal verification steps (must include re-running calculator unit + at least one full roundtrip):**
- pnpm nx test calculator -- --watchAll=false (lib) + pnpm nx test api --testPathPattern=calculator (api svc) -- multiple times (3+).
- Full: pnpm e2e (headless) multiple times after any changes; also pnpm test && pnpm build && pnpm typecheck && pnpm e2e cycle per FINAL_REVIEW:53.
- For real parity E2E: add golden JSON fixtures (serialize baseInput from calculator.spec.ts + its calculate() output as expected; also from coresharepay.py samples); dedicated test using @playwright/test request to POST /auth/register (real), then create house/bill/kwh/tenants/subs via /houses API (bypassing current mock UI for reliable seeding), POST /:id/calculate, deep equal to golden (exact €, new_main_kwh, left_over1 shape, sub_house_with keys/tenant_value vs value, 3dp, "id-Name"). Drive at least one full UI journey only after houses.service wired to real HTTP + calc-report does http POST calc (or use injected token + navigate).
- Integrate with nx: extend playwright.config with globalSetup that spawns api (NODE_ENV=test but using postgres test db or dedicated sqlite file, not :memory:) + wait for healthy; use docker compose test profile or testcontainers for postgres isolation. Assert on calc-report DOM: exact strings (e.g. expect(locator('table').first()).toContainText(['€xxx.xxx', '2-Name', 'left_...'])), row counts, collapsible left_over details.
- Property-based: fast-check generators for valid periods/tenants/subs (respecting <kwh, date ranges) -> assert roundtrip (direct lib === via persisted API load) + invariant (sum shares ≈ bill within 0.001 rounding, no NaN, positive, left_over only on uncovered days).
- Re-run e2e headless + lib units 3x after seeding any calc change. Cover all listed journeys: happy mixed, overlapping, partial left_over, subkwh boundary, zero-day, date-destructive (verify tenants cleared, calc reflects).

**Open questions / assumptions:**
- Assumes frontend wiring to real backend (auth + houses http + calc-report fetch) will happen before claiming E2E covers "full user journeys against the real backend" (per baseline gap + auth.service:21 comments). Without it, even "comprehensive" playwright would only re-test mock.
- Test DB strategy: current docker postgres for prod; for e2e recommend explicit test postgres (not relying on in-mem sqlite which can't serve cross-process). Flakiness mitigation: unique per-test house names + cleanup, or full truncate in beforeEach via test helper.
- No actual golden JSON or py fixture script present in tree (only intent/docs); adding them + deep-equal is highest value add for "exact calc output parity assertions (golden + property)".
- Would current example.spec.ts even pass if api were started? (signup uses fake, no real JWT for later guarded routes if wired).

## AGENT-EVAL: FAQ-README-SEED @ 2026-06-16T18:42:00Z
Analysis (cites specific files/lines or old logic):
- Read FEATURE_EVAL_SYNC.md:25 (lists exactly the 4 open issues: #70 FAQ, #18 README, #13 Print PDF, #12 Auto-fill dates), README.md:1-61 (logo + origin story from shared neighbor meter + 3 feature scenarios + 🚩sub-house rule for separate meter rooms + 2026 Migration Notice claiming "fully migrated", "100% bill splitting logic preserved in `libs/calculator`", pointers to MIGRATION etc), templates/landpage/faq.html:58-130 (two accordions "Account & Billing" + "Website Issues" but all content is Bootstrap template placeholders/lorem, no real Qs), apps/web/src/app/features/landpage/landpage.component.ts:46 ("Features from index/about/pricing/faq condensed"), :74-80 (only "Pricing & FAQ Highlights" + 3 high-level bullets, no expandable Q&A, "Demo Video Placeholder" at :39), MIGRATION.md:7-83 ("Original Architecture" section: HouseNameModel meter, SubHouseNameModel sub_meter + sub_kwh validation "sum(sub_kwh) < main.kwh (strict, not <=)", tenants date constrained + "On bill date change → destructive delete of all tenants", CoreSharePay steps: split_bill new_main_kwh = main - sum(with-meter), check_if_which_sub... partition without_kwh belong to main pool, create_range... + get_tenants_by_day using "left_{house}_{date}" sentinels for absent, calc_1/calc_2 + left_over1 for empty days, "Divide-by-1 when 0 tenants on a day", "Residents only pay for days they lived there", "Empty days (no tenants) accumulate in "left_over" buckets"), PERFORMANCE_SECURITY_AUDIT.md:46 ("Full OAuth parity ... no social auth yet"), :71 ("Calculator is pure sync CPU. For a 90-day bill + 12 residents total: microseconds."), apps/api/src/houses/houses.controller.ts:167-172 (real POST /:id/calculate using loadFullHouseForCalc + CalculatorService), apps/api/src/houses/houses.service.ts:269-299 (setSubKwh comment + exact error for sum > or === mainKwhNum mirroring old), :358-375 (loadFullHouseForCalc), apps/web/src/app/core/houses.service.ts:221-254 (mock calculate builds CoreInput then `new BillCalculator(input).calculate()`, never hits real HTTP despite interceptor), libs/calculator/src/lib/calculator.ts:78-96 (splitBill newMainKwh deduction), :192-255 (getTenantsByDay inserts `left_${houseName}_${dateStr}` sentinels and dedup logic), :291/394 (const checkZero = names.length === 0 ? 1 : names.length), :344-360/442-457 (left_over1 aggregation per house with details_date + total + days_left_over), calculator.spec.ts:58-68 (left-over test case exists but minimal).
- 4 open issues discovered/confirmed via FEATURE_EVAL_SYNC + cross-refs in FINAL_REVIEW/CHANGELOG (no other sources like .github/issues present).
- Old Python landpage/ + templates retained but not source of truth; Angular SPA is the current landpage (routes to LandpageComponent at '').

Risks (esp. exact output drift, validation bypass, left_over sentinel handling, sub-kwh strict < rule, date range destructive deletes):
- README migration notice + "fully migrated" language risks overclaiming "full feature parity" (sync baseline explicitly calls out "Frontend gap", "partial wiring", "real calculate endpoint loading aggregate" still in flight); new GitHub visitors (2 stars, 4 issues) may assume working auth+API+social like old allauth.
- FAQ content (for #70) MUST NOT hallucinate completeness: no videos (only placeholder + old YT embed in templates/landpage/index.html:55), no screenshots in repo, current demo is localStorage-only (houses.service), report parity is via direct lib call in mock not yet via POST /calculate; must accurately explain left_over (accumulated owner cost, not redistributed), why sub kwh sum cannot reach total (enforced at setSubKwh + in calc split), why partial periods matter (destructive delete + daily presence maps).
- Optional demo seed ("Load Demo House"): high rot risk if hardcodes ids or bypasses (e.g. direct to calculate() without setBill/setKwh which enforce range + positive + destructive). If added to web mock it will diverge once real API wiring (houses.controller + service) is used; if real POST must use full DTO path + loadFullHouseForCalc to preserve left_over/sub partitioning invariants. Seed fixture must produce non-trivial left_over1 + sub_house_with + sub_house_without simultaneously to be useful for onboarding.
- Date destructive + strict sub-kwh in both mock (houses.service:92-93,168-169) and api (service:116-122,295-299) must be documented in FAQ so users aren't surprised when adding bill clears data or sub kwh rejected at ==.
- No user-facing PERFORMANCE section yet; calc speed is excellent but irrelevant until real API used from SPA.

Your adjusted Priority (P0-P3) / Complexity (L/M/H):
- P2 / M  (onboarding/docs polish for discoverability on GitHub; does not touch calc core or invariants; content work + light component for FAQ + possible seed button, but wiring dependency makes full value gated on frontend-api integration).

Recommended minimal verification steps (must include re-running calculator unit + at least one full roundtrip):
- `pnpm test calculator` (or nx test libs/calculator) + extend baseInput in spec temporarily to force left_over1 + mixed with/without kwh subs and assert exact structure/values (no drift).
- Full roundtrip: use houses.service (mock) or (future) real POST to /calculate with canonical fixture from MIGRATION/FINAL (main 300kwh €120, overlapping main tenants, 1 sub 80kwh with tenant, 1 sub no-kwh, short-stay tenant) → deep-equal output shape + €.xxx + positive + left_over present where expected; also run via api calculator.service.runCalculationForHouse after entity load.
- `pnpm build web && pnpm build api` (and typecheck) to ensure landpage/readme changes don't break.
- For docs: manual scan README for claims vs reality (e.g. no "social login" mention, call out "demo uses in-browser storage; full persistence requires API wiring"), link-check any added anchors; if FAQ area implemented later, render and confirm Qs match MIGRATION rules without promising videos/screenshots that don't exist.
- docker compose config + (non-prod) up to validate self-host instructions; verify no demo seed bypasses (e.g. addTenant after bill only).
- Cross-check against coresharepay.py invariants (if still in tree) + FINAL_REVIEW backward compat examples.

Open questions / assumptions:
- Will FAQ be a dedicated route (/faq) + component (Angular) or embedded section in landpage + static md? (old Django had /landpage/faq but was template-only).
- Should "Load Demo" seed target the mock only for now, or be deferred until houses.service is swapped to real HttpClient calls (see auth.interceptor)?
- readme.so visual approach recommended over pure md? Yes for badges/hero/screenshots/diagram, but must be manually maintained (no auto-gen in current setup); keep origin story condensed vs walls of text.
- Assumption: evaluation stays truthful per LLM pitfall warning (no claiming "the app supports X" where mock-only or pending); 2-star repo needs scannable Q&A + accurate status to reduce issue noise on #12/#13.
- No AGENTS.md or similar found constraining docs style.

(End of AGENT-EVAL: FAQ-README-SEED)

## AGENT-EVAL: PRINT-PDF-AUTOFILL @ 2026-06-16T18:42:00Z
**Analysis (cites files/lines/old logic):**
- calc-report.component.ts (full): renders info table + main_house / sub_house_without / left_over1 (collapsible via showLO1 + [class.show] on .collapse, details_date tables) + sub_house_with + left_over_with, using helpers that mirror calc_bill/calc_main_house.html + with_kwh.html structure (e.g. captions, thead-dark, table-success/secondary/danger rows, period strings as "start to end", values as €.xxx). Calls svc.calculate(id) which builds CoreInput (startDateBill etc as yyyy-MM-dd strings) + new BillCalculator(). No print/export, no date helpers.
- house-detail.component.ts (lines 34-38 bill dates reactive, 63-68 main tenant dates reactive FormGroup, 92-97 sub ngModel standalone newSubStart/End per subId): date <input type="date">. On save/add delegates to svc.setBill/addTenant/addSubTenant which compute days = Math.floor((e-s)/86400000) + range checks vs bill (exact mirror of old Django form clean + model constraints).
- libs/shared/src/index.ts:88 (daysBetween: identical Math.floor diff / (1000*3600*24) on Date parse of 'YYYY-MM-DD'), used by apps/api/src/houses/houses.service.ts:99,191,229,325 for all bill/tenant/sub days (and CalculatorService builds CoreInput from entity dates/days without re-calc).
- houses.service.ts (web mock):219 (calculate builds CoreInput with pre-stored days from tenant data; never recomputes inside calc), setBill:85, addTenant:127, addSubTenant:192 use same floor math. Matches shared + calculator.ts:55 (parseISO + addDays for internal ranges, but daysBill/days passed in from outside).
- Legacy: templates/houses/calc_bill/calc_main_house.html:17 (info table), :74 (main), :82 (sub without), :116 (left_over1 collapse #collapseWithoutKWH with details_date + totals), with_kwh.html:48 (left with); report.html wraps in masthead + "Calculate" btn. calc_1.css: minimal table. MIGRATION:84 "rich tables ... exact values, structure, copy of 'Due Value'"; "full feature parity".
- No print/pdf anywhere in TS (grep hits only legacy static css landpage + fa icons). styles.scss minimal; masthead inlined in detail/list/create (app.component nav/footer always present).
- GitHub #13/#12 directly map.

**Risks (exact output drift, validation bypass, left_over sentinel, date formats, sub-kwh, destructive):**
- Does NOT touch sacred calc path: print is render-only of existing result; autofill only pre-populates form fields before add* which go through identical validation+days computation as before (then stored days feed CoreInput). Invariant preserved.
- Date formats: type=date always emits 'yyyy-MM-dd' (matches CoreInput spec, shared daysBetween, parseISO, legacy storage, bill.start_date_bill etc). Autofill must assign bill strings directly (no new Date().toISOString().slice which is safe here but avoid any TZ drift risk on boundary dates).
- Print must include the left_over details: collapsible (showLO1=false default; uses bootstrap .collapse + .show but no JS bootstrap in Angular web; only [class.show]). @media print { .collapse { display:block !important; } .btn[data-toggle], a.close {display:none} } required, else printed PDF would omit the details_date tables + summary (non-obvious vs naive window.print on current DOM). Legacy calc pages rendered expanded? but print target is visual parity of full report tables.
- Auto-fill days: must NOT compute/display days client-side separately (or if preview, MUST call exact daysBetween from shared to match what svc will compute on add and what calculator receives). Prefer: just set start/end strings, let existing add path compute/validate/store days (server source of truth when wired).
- Visual/CSS parity for print: current Angular tables use bootstrap classes (table-sm, thead-dark, bg-info) that landpage.css/bootstrap provide in legacy but web has only tiny styles.scss + inline. Print may look different without full vendor css; also app nav/footer always in DOM (need hide in @media print). No masthead on calc-report (unlike legacy calc pages) — this is actually cleaner for PDF.
- New deps risk: LLM pattern warns against jsPDF/html2canvas/puppeteer for "print to PDF". Minimal window.print() + "Save as PDF" in dialog + targeted @media print CSS is sufficient, zero deps, exact visual match to rendered (no canvas re-render drift).
- Future CSV/export: print is visual report fidelity (user #13); separate from structured export. Place button distinctly to avoid confusion.
- Other: SSR/hydration: window.print() is browser-only (fine, feature client). a11y: add aria-labels on new buttons; ensure focusable. Collapsible left_overs use click handlers — ok. Sub tenants use per-sub ngModel records; autofill needs to target the right subId.
- Destructive bill change etc untouched.

**Adjusted Priority / Complexity:** P1 (high usability ROI per original user complaints on tedious date entry for full-period residents; print is quick win for sharing reports). Complexity: L (very low; ~10-15 LOC CSS + 2-3 buttons + 1 method each; no calc touch, no new imports beyond existing CommonModule).

**Recommended minimal verification steps (must include re-running calculator unit + at least one full roundtrip):**
- Re-run: `pnpm nx test calculator` (and full pnpm test) — confirm no change (autofill/print never reach here).
- Manual: 1. load/create house+ bill (full period) +kwh + tenants/subs with partial dates; 2. use autofill buttons on main tenant form + each sub tenant form — assert dates patched to bill start/end, then Add succeeds, days match bill.days_bill; 3. go to calc, expand left_overs, click Print button -> browser print preview -> Save as PDF; visually inspect PDF matches rendered tables (info header, all tenant periods/days/€values, all left_over details tables with sentinels, with-kwh sections) + no masthead/nav, full width tables. Compare key numbers to legacy Django calc output for same data.
- Full roundtrip: after autofill-add, call svc.calculate (or real /calculate), assert output.main_house etc periods/days/values unchanged vs manual date entry for same full bill span.
- E2E: extend web-e2e to cover (a) date autofill flow + calc match, (b) report page has print btn (smoke; full print dialog hard in headless but can assert DOM state pre-print).
- Also: ensure print includes left_overs even if collapsed (test with CSS).
- When real API wired: same flow against POST houses/:id/calculate + tenant endpoints (uses shared daysBetween).

**Open questions / assumptions:**
- Assume bootstrap CSS (or equiv table styles) will be present for report parity (currently implicit); may need to import or replicate minimal table rules for web standalone.
- Autofill only for tenant/sub periods (not bill itself, per issue).
- No "auto days display" update in form until submit (current UX doesn't show live days on tenant add).
- Print v1 is browser Save-as-PDF only; no server PDF gen or auto-download.

(End of AGENT-EVAL: PRINT-PDF-AUTOFILL)

## IMPL-START-ORCHESTRATOR @ 2026-06-16T19:10:00Z
**Material update (wiring of Feature 1 begun autonomously post all phases)**:
- Fixed proxy target :3000 (Nest), wired authInterceptor in app.config.ts, converted AuthService login/signup to real POST /api/auth/* (returns access_token; stores real JWT for guards + interceptor).
- Enhanced core/houses.service.ts with hasRealToken, mapHouseFromApi (camel->snake for UI compat), listReal, calculateReal (POST /houses/:id/calculate — the authoritative Nest path through loadFull + CalculatorService + pure BillCalculator), and updated calculate() to fire real when token while preserving the entire local CoreInput + new BillCalculator path verbatim (demo/mock outputs remain byte-identical for same in-memory data).
- Updated calc-report ngOnInit to prefer calculateReal (async, falls back), house-list to call listReal on init.
- **Verification discipline followed**: after each batch ran full `nx run-many -t typecheck` (clean on 4 projects) + `nx test calculator` (3/3 green including left-over accumulation test, no drift). Mock path untouched. No edits to libs/calculator, Nest calc service, entities, or output shapes.
- Per agent guidance + DIG_DEEP_REVIEW: this unblocks real JWT + persisted data + /calculate for parity proof while keeping existing demo working.
- Next (not done in this start): full async conversion + real mutations in house-detail (setBill etc via real DTOs), E2E update, subKwh/tenant forms against real, golden roundtrips in tests.
- No other files changed except the required sync append. All constraints (parity, verification cycles, minimal, no questions) observed.

**Risks noted during edits**: same as agents (mapping in calculateReal not yet used for mutations, component async incomplete, proxy for e2e still needs globalSetup for api+db). Calculator tests + typecheck remain the gate.

(End of IMPL-START)
